<UserControl x:Class="PasswordPlugin.PasswordBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PasswordPlugin"
             mc:Ignorable="d" >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="30px" />
            <RowDefinition Height="30px" />
        </Grid.RowDefinitions>
        <TextBox x:Name="x_description" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"
      AcceptsReturn = "True"
      AcceptsTab = "True"           >Lorem Ipsum</TextBox>
        <TextBox Grid.Row="1" x:Name="x_question"></TextBox>
        <TextBox Grid.Row="2" x:Name="x_answer"></TextBox>
    </Grid>
</UserControl>
