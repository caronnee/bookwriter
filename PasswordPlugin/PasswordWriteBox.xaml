<UserControl x:Class="PasswordPlugin.PasswordWriteBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PasswordPlugin"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="30px" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <TextBox x:Name="description" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"
      AcceptsReturn = "True"
      AcceptsTab = "True"           >Lorem Ipsum</TextBox>
        <TextBox Grid.Row="1" x:Name="x_question"></TextBox>
        <!-- Column II -->
        <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
            <StackPanel x:Name="hintPanel">
                <local:HintItem Grid.Column="1"></local:HintItem>
            </StackPanel>
        </ScrollViewer>
        <Button Grid.Column="1" Grid.Row="1" Content="Add" Click="AddButton" />
    </Grid>
</UserControl>
