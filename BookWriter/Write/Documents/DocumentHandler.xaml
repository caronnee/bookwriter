<UserControl x:Class="MyBook.Write.Documents.DocumentHandler"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MyBook.Write.Documents"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
  <StackPanel>
    <TextBox x:Name="x_name" Text="{Binding Name}" />
    <Grid>
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="*" />
      </Grid.ColumnDefinitions>
      <StackPanel Grid.Column="0">
        <Label>Document content</Label>
        <ScrollViewer>
          <TextBox x:Name="x_content" AcceptsReturn="True" Text="{Binding Content}"></TextBox>
        </ScrollViewer>
      </StackPanel>
      <DockPanel Grid.Column="1">
        <Label>Document details</Label>
        <ScrollViewer>
          <TextBox x:Name="x_details" AcceptsReturn="True" Text="{Binding Misc}"/>
        </ScrollViewer>
      </DockPanel>
    </Grid>
  </StackPanel>
</UserControl>
