<TextBox x:Class="MyBook.Pages.Write.Text.WritingBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MyBook"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" Width="300"
      MaxLines ="{Binding Source={x:Static local:Settings.LineCount}}" 
      MinLines = "{Binding Source={x:Static local:Settings.LineCount}}" 
      TextWrapping ="Wrap"
      AcceptsReturn = "True"
      AcceptsTab = "True"
      VerticalScrollBarVisibility = "Auto"
         >
    <TextBox.ContextMenu>
        <ContextMenu>
            <MenuItem Header="Add to bookmark" ItemsSource="{Binding Bookmarks}" DisplayMemberPath="Name">
            </MenuItem>
            <MenuItem Header="Copy"></MenuItem>
            <MenuItem Header="Paste"></MenuItem>
        </ContextMenu>
    </TextBox.ContextMenu>
</TextBox>
