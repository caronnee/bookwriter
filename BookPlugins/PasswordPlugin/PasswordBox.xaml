<UserControl x:Class="PasswordPlugin.PasswordBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PasswordPlugin"
             mc:Ignorable="d" >
    <DockPanel LastChildFill="True">
        <DockPanel LastChildFill="True" DockPanel.Dock="Bottom" x:Name="x_answer_container">
            <Button DockPanel.Dock="Right" x:Name="x_send_answer">Answer</Button>
            <TextBox x:Name="x_answer"></TextBox>
        </DockPanel>
        <DockPanel>
            <TextBlock DockPanel.Dock="Top" ScrollViewer.CanContentScroll="True" TextWrapping="Wrap" x:Name="x_description"> IpsumLorem IpsumLorem IpsumLorem IpsumLorem IpsumLorem IpsumLorem IpsumLorem Ipsum</TextBlock>
            <TextBlock x:Name="x_final_answer" DockPanel.Dock="Top" Visibility="Collapsed">the answer</TextBlock>
        </DockPanel>
    </DockPanel>
</UserControl>
