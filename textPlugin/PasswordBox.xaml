<UserControl x:Class="PasswordPlugin.PasswordBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PasswordPlugin"
             mc:Ignorable="d" >
    <StackPanel>
        <TextBox TextWrapping="Wrap" MaxLines="100" IsEnabled="False">Lorem Ipsum</TextBox>
        <TextBox x:Name="Answer" PasswordBox.PasswordChanged="Answer_PasswordChanged"></TextBox>
        <Label x:Name="hint">Hint</Label>
    </StackPanel>
</UserControl>
